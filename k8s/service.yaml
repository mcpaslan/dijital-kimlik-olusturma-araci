apiVersion: v1
kind: Service
metadata:
  name: dijital-imza-service
  labels:
    app: dijital-imza
spec:
  type: NodePort
  selector:
    app: dijital-imza
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30005
